# Help Sathi

Project structure:

- `Client/` contains the React Native Expo app
- `model/` contains the CNN model and related ML code

Client commands:

```bash
cd Client
npm install
npx expo start
```
